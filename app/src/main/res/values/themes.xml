<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DailyBand" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="windowNoTitle">true</item>

        <item name="colorPrimary">@color/test_main_clolr</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/midgreen</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/test_main_clolr</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="EditTextGreyStyle" parent="Widget.Design.TextInputLayout"> // important : parent 를 이것으로.

        <item name="colorControlNormal">@android:color/transparent</item>  // unFocus 상태일때 밑줄 색.
        <item name="boxStrokeColor">@android:color/transparent</item> // Focus 상태일때 밑줄 색.
        <item name="android:textCursorDrawable">@color/mainblack</item>  // cursor 색.

        <item name="android:textColorHint">@color/gray_sto</item>  // hint 색
        <item name="hintTextColor">@color/gray_sto</item>   // floating hint 색

        <item name="errorTextColor">@color/midgreen</item>

    </style>
</resources>